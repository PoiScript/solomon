{
  "name": "solomon",
  "version": "0.0.2",
  "description": "Blog Site for PoiScript",
  "license": "MIT",
  "repository": "git@github.com:PoiScript/Solomon.git",
  "author": "PoiScript <poiscript@gmail.com>",
  "scripts": {
    "setup": "bash ./scripts/setup.sh",
    "ng:start": "cd ng && yarn start",
    "ng:build": "cd ng && yarn build",
    "react:start": "cd react && yarn start",
    "react:build": "cd react && yarn build",
    "generate:post": "node scripts/generate-post",
    "generate:link": "node scripts/generate-link",
    "generate:rss": "node scripts/generate-rss"
  },
  "private": true,
  "dependencies": {
    "express": "^4.15.3",
    "marked": "^0.3.6",
    "node-sass": "^4.5.3",
    "pug": "^2.0.0-rc.2",
    "rss": "^1.2.2"
  },
  "devDependencies": {
    "standard": "^10.0.2"
  }
}
