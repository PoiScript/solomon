/* tslint:disable:no-unused-variable */
import {KeywordPipe} from "./keyword.pipe"

describe('KeywordPipe', () => {
  it('create an instance', () => {
	  let pipe = new KeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
