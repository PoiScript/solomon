/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StepPipe } from './step.pipe';

describe('StepPipe', () => {
  it('create an instance', () => {
    let pipe = new StepPipe();
    expect(pipe).toBeTruthy();
  });
});
