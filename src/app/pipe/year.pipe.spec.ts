/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { YearPipe } from './year.pipe';

describe('YearPipe', () => {
  it('create an instance', () => {
    let pipe = new YearPipe();
    expect(pipe).toBeTruthy();
  });
});
