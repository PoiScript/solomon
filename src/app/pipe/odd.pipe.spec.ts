/* tslint:disable:no-unused-variable */
import {OddPipe} from "./odd.pipe"

describe('OddPipe', () => {
	it('create an instance', () => {
		let pipe = new OddPipe();
		expect(pipe).toBeTruthy();
	});
});
