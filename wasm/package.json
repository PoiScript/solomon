{
  "name": "@solomon/wasm",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:web": "wasm-pack build -t web -d ../web/pkg -- --no-default-features --features web",
    "build:worker": "wasm-pack build -t no-modules -d ../worker/pkg -- --no-default-features --features worker"
  }
}
